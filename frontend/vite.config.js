/*
 * ARCHIVO COMPARTIDO
 * Configuración básica de Vite.
 * No modificar sin avisar a Personas 3, 4 y 5.
 */
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
})
